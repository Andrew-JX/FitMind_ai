{
  "name": "@fitmind/client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\" && vite build",
    "dev": "vite",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src vite.config.ts",
    "test": "vitest run --passWithNoTests src/**/*.test.ts",
    "test:e2e": "playwright test",
    "verify:e2e-lifecycle": "node scripts/verify-e2e-lifecycle.mjs",
    "verify:e2e-exit": "node scripts/measure-e2e-exit.mjs 3"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "typescript": "^5.9.3",
    "vite": "^7.1.12"
  }
}
