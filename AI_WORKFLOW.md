# AI 辅助开发工作流（FitMind AI 项目专用）

> **核心理念**：AI 不是替你写代码的工具，是放大你工程能力的工具。 你的角色是**架构师 + 审稿人 + 验证者**，AI 的角色是**执行者 + 草稿生成器 + 文档机器**。 你只要一不小心把自己变成「按回车的人」，项目就完蛋。

------

## 一、Claude Code 和 Codex 的分工

很多人把这两个工具当成可替换品，**这是错的**。它们的特性不同，分工不同。

### Claude Code（Anthropic）

**适合**：

- 大块代码生成（一个完整模块、一个完整页面）
- 需要长上下文理解的任务（读完整个 docs/ 再动手）
- 需要严格遵循项目规则的任务（AGENTS.md 约束下写代码）
- 复杂重构、跨多文件改动
- 写文档、写测试

**不适合**：

- 单行补全（太重）
- 只改一行 typo（太慢）

**心智模型**：把 Claude Code 当作一个**初级到中级工程师**。你给它任务，它执行；你审稿，它修。

### Codex（VSCode Codex 插件 / OpenAI）

**适合**：

- 快速代码补全（写到一半，Tab 一下）
- 短小的工具函数生成（一个 utils 函数、一个正则）
- 内联问答（这一行代码什么意思）
- 写测试用例（已知函数签名，让它补 test）

**不适合**：

- 一次写一个大模块（容易跑偏）
- 需要严格遵循项目架构的任务（它对项目长上下文不敏感）

**心智模型**：把 Codex 当作一个**特别快的实习生**。手感型工具，少废话，多 Tab。

### 在 FitMind 项目里的具体分工

| 任务                                  | 用谁                          | 原因                     |
| ------------------------------------- | ----------------------------- | ------------------------ |
| 写 `docs/` 下面所有文档               | Claude Code                   | 需要长上下文 + 风格统一  |
| 数据库 DDL + Migration                | Claude Code                   | 需要看完 schema 再动手   |
| Express 路由 + 控制器搭骨架           | Claude Code                   | 跨文件改动               |
| 单个 utils 函数 / 类型定义            | Codex 补全                    | 局部任务                 |
| React 组件骨架                        | Claude Code                   | 需要看 design system     |
| 组件内的样式微调、JSX 小改            | Codex Tab 补全                | 即时反馈                 |
| 计算层算法实现（fatigue / volume 等） | Claude Code                   | 需要先讨论方案再实现     |
| 算法的单元测试用例                    | Codex                         | 函数签名已知，补 test 快 |
| Tool Calling 循环这种核心代码         | **你自己 + Claude Code 配合** | 必须懂每一行             |
| SSE 状态机这种核心代码                | **你自己 + Claude Code 配合** | 同上                     |
| 写 README、写面试稿                   | Claude Code                   | 长文档                   |
| 写 commit message                     | Codex                         | 一行就够                 |

**记住**：项目核心代码（Tool Calling 循环、SSE 解析、状态机）不能完全甩给 AI。这些是你面试时要被追问的，必须每一行都懂。

------

## 二、Plan-then-Execute 工作流（你已经在用，但要做对）

你说你「先用 plan 再做实施」——方向对，但大多数人 plan 阶段做得太敷衍。下面是真正可用的版本。

### 标准四步法

```
需求 → Plan → Confirm → Execute → Verify → Document
```

每一步具体做什么：

#### 步骤 1：需求（你做）

用一段话写清楚你要什么。**烂需求示例**：

> 帮我实现 fatigue 计算函数

**好需求示例**：

> 我要在 `server/src/services/analytics/fatigue.ts` 里实现一个函数， 输入是 user_id 和 muscle_group_id，输出是该用户该肌群最近 7 天的疲劳分数（0-10）。 数据来自 sets 表（join workouts 拿日期，join exercise_muscles 拿 contribution_weight）。 当前思路：每组负荷 = volume × rpeFactor × muscleContribution × decay(daysAgo)， τ 默认 3.5 天，最后归一化到 0-10。 已写好的相关代码：types/training.ts 的 TrainingSet 接口、constants.ts 的 FATIGUE_TAU。 项目规范见 AGENTS.md（用 TS 严格模式，所有函数要有 JSDoc，公式参数从 constants 读）。

**好在哪**：路径明确、输入输出明确、思路明确、关联代码明确、规范明确。

#### 步骤 2：Plan（让 AI 做）

**关键：不要直接让 AI 写代码，让它先给方案**。

固定 prompt 模板：

```
请先不要写代码，给我一个执行计划：

1. 你打算分几个步骤完成这个任务
2. 涉及哪些文件（新建 / 修改）
3. 关键的设计决策有哪些（比如 fatigue 衰减系数怎么定）
4. 有没有需要我先确认的不确定点
5. 风险和边界情况（比如用户 7 天没训练怎么办）

如果有 2-3 种方案可选，列出来对比，我选完你再写。
```

#### 步骤 3：Confirm（你做）

看 AI 给的 plan，问自己 4 个问题：

1. **它真的理解需求了吗？** 如果它的理解和你想的不一样，立刻打住，不要让它写下去。
2. **方案有没有偷懒？** 比如它说「用简单的线性衰减」，你要追问「为什么不用指数衰减，工程权衡是什么」。
3. **边界情况覆盖了吗？** 用户 7 天没训练、用户刚注册、肌群没匹配上——这些它想到了吗？
4. **会不会破坏项目架构？** 它要新建文件吗？放对目录了吗？依赖正确吗？

**这一步最容易偷懒，但回报最高**。我见过的所有把 AI 用废的人，都是 Plan 阶段不审就让它直接 Execute，后面项目越来越乱。

#### 步骤 4：Execute（让 AI 做）

确认完 plan 之后再说：

```
方案确认。开始实现，要求：
- 严格按 plan 执行
- 每个文件改动给我一个简短说明
- 遇到 plan 没覆盖的情况停下来问我
- 最后总结改了哪些文件、有什么风险点、需要我重点 review 哪里
```

**一次只做一个 plan 的事情**。如果一个 plan 涉及超过 5 个文件，先拆。

#### 步骤 5：Verify（你做）

代码出来之后**绝对不能直接 commit**。流程：

1. **跑通**：`npm run build`、`npm run lint`、`npm test`
2. **手动跑一遍**：开浏览器、postman 调接口、看实际效果
3. **逐文件 review**：不是「看一眼」，是真的读懂每一行
4. **追问自检**：随便挑两行代码，问自己「面试官问我为什么这么写，我能讲清楚吗？」

**做不到「能给面试官讲清楚」的代码不算你的。** 这一句话刻在脑子里。

#### 步骤 6：Document（你 + AI 配合）

每完成一个有意义的任务，更新 `docs/ai-decisions.md` 或 `docs/troubleshooting.md`，记录：

- 做了什么
- 遇到什么坑
- 怎么解决的
- 为什么这么选

让 AI 帮你润色，但**核心要点必须你自己写**——这些是面试素材库。

------

## 三、新对话开场模板（保命级别）

每次开 Claude Code 或 Codex 新对话，**第一条消息固定模板**。这一步省了，后面 AI 写出来的代码就开始打架。

### Claude Code 开场模板

```
你是 FitMind AI 项目的协作工程师。开始任何任务前，先做这 3 件事：

1. 阅读 AGENTS.md，确认项目规则
2. 阅读 docs/PROJECT_BRIEF.md 和 docs/architecture.md，确认项目定位和分层
3. 阅读和当前任务相关的 docs/（比如做后端就读 api-contract.md，做数据库就读 db-schema.md）

读完后，简要总结你理解到的项目约束（不超过 5 条），然后等我下一条消息给你具体任务。

如果发现任何文档缺失或冲突，立刻指出，不要假装看懂。
```

### Codex 开场模板（短一些）

Codex 主要做即时补全，开场 prompt 简化：

```
项目：FitMind AI（React + TS + Node + PostgreSQL）
当前文件：[路径]
约束：参考 AGENTS.md（严格 TS、函数必须有 JSDoc、UI 层不能直接调 API 层）
任务：[具体任务]
```

------

## 四、Anti-pattern 清单（这些事千万别做）

### 1. 不读 plan 就让 AI 直接写

**症状**：你说「帮我实现 X」，AI 直接给 200 行代码，你点了 Apply。

**后果**：第三周你发现项目里有三种命名风格、两套 API 客户端、四个相似的 utils 函数。

**正确做法**：永远先 Plan-Confirm-Execute。

### 2. 一次让 AI 改超过 5 个文件

**症状**：「帮我把整个登录系统加上」，AI 一次改了 12 个文件。

**后果**：你 review 不过来，bug 藏得到处都是。

**正确做法**：拆成「数据库 schema → 后端路由 → 前端表单 → 前端跳转」四次任务，每次审完再下一次。

### 3. 不跑测试就 commit

**症状**：AI 写完，看起来对，commit。

**后果**：上线发现 type error 或 runtime error，回滚。

**正确做法**：build + lint + 手动跑核心路径，三关都过才 commit。

### 4. 把核心代码全甩给 AI

**症状**：Tool Calling 循环 100% 让 AI 写，你都不知道 messages 数组怎么拼的。

**后果**：面试官问「tool_use 块和 tool_result 块怎么对应」，原地爆炸。

**正确做法**：核心代码（Tool Calling 循环、SSE 解析、状态机、计算层算法）必须你 + AI 一起写，每一行你都懂。

### 5. 让 AI 写出和原项目风格不一致的代码

**症状**：你的项目用 `camelCase`，AI 突然写 `snake_case`；你的项目用箭头函数，AI 写 `function` 关键字。

**正确做法**：AGENTS.md 里写清规范，每次新对话 AI 第一步必须读它。

### 6. 不记录踩坑

**症状**：debug 了 3 小时解决一个问题，下次又遇到，又 debug 3 小时。

**正确做法**：`docs/troubleshooting.md` 每次都记。这本身就是面试素材库。

### 7. 简历驱动开发

**症状**：还没做主线，先想「这里加个 RAG 简历好看」。

**正确做法**：主线门控不通过，扩展不许动。

------

## 五、推荐的每日工作流

假设你今天有 4 小时投入项目：

| 时间        | 事情                                            | 用谁             |
| ----------- | ----------------------------------------------- | ---------------- |
| 0:00 - 0:15 | 复盘昨天进度，确定今天 3 个具体任务             | 你               |
| 0:15 - 0:30 | 写第一个任务的需求 + 让 AI 出 plan              | Claude Code      |
| 0:30 - 1:30 | 审 plan、confirm、execute、verify               | 你 + Claude Code |
| 1:30 - 1:45 | 跑测试、手动验证、commit                        | 你               |
| 1:45 - 1:55 | 更新 docs/ai-decisions.md 或 troubleshooting.md | 你（AI 润色）    |
| 1:55 - 2:00 | 短休息                                          |                  |
| 2:00 - 3:30 | 第二个任务（同上流程）                          |                  |
| 3:30 - 4:00 | 第三个任务（小任务，用 Codex 补全为主）         |                  |

**关键节奏**：每 1.5 小时一个完整任务循环。不要一口气做 3 小时不停。

------

## 六、你应该建立的文档体系（这个项目专用）

```
fitmind-ai/
├── AGENTS.md                    # AI 编码助手必读规则
├── README.md                    # 项目介绍（最后写）
├── docs/
│   ├── PROJECT_BRIEF.md         # 项目说明书 / 项目宪法
│   ├── architecture.md          # 前后端分层、模块划分
│   ├── db-schema.md             # 数据库设计 + DDL
│   ├── api-contract.md          # 后端接口约定
│   ├── ai-decisions.md          # 关键技术决策记录
│   ├── troubleshooting.md       # 踩坑日记
│   └── project-study-guide.md   # 面试问答稿 + 按阶段讲稿（最后写）
└── ...
```

**写文档的顺序**：

1. **现在**：PROJECT_BRIEF.md + AGENTS.md（让 AI 有抓手）
2. **阶段 0**：architecture.md + db-schema.md + api-contract.md + ai-decisions.md（含初始决策）
3. **阶段 1 开始**：troubleshooting.md（边踩边记）+ ai-decisions.md（边做边补）
4. **打磨阶段**：README.md + project-study-guide.md

------

## 七、面试讲 AI Coding 时的标准回答

后面面试一定会有「你怎么用 AI 辅助开发的？」这种问题。准备一段标准回答：

> 我用 AI Coding 不是把它当作代码生成器，而是当作团队里一个初级工程师。我的工作流是 Plan-Confirm-Execute-Verify 四步：
>
> 第一步，我把需求拆清楚，包括目标、输入输出、相关文件、规范约束。 第二步，让 AI 先出方案不写代码，我审方案，确认它真的理解了，再让它执行。 第三步，AI 写完我必须自己跑通、自己 review、自己能给别人讲清楚每一行——做不到这一步的代码不算我的。 第四步，把每次的关键决策和踩坑记到 docs 里，沉淀成项目知识库。
>
> 我项目里有一个 AGENTS.md 文档，写明项目分层、命名规范、不能跨层 import 这些约束，AI 每次开始任务前必须读它，避免它写出和项目风格不一致的代码。我也明确分工——核心代码比如 Tool Calling 循环和 SSE 状态机我必须自己懂每一行，UI 样式微调和 utils 工具函数可以让 Codex 快速补全。
>
> 我觉得用 AI 的核心是：你必须始终是架构师和审稿人，不能一不小心把自己变成按回车的人。

**这一段背熟，面试加分。**

------

## 八、心态校准

最后一段不写代码相关的，写心态。

你 5 月开始这个项目，10 周后做完，期间你会经历：

- 第 2 周：兴奋，进度快
- 第 4 周：开始遇到 AI 生成的代码自己看不懂的恐慌
- 第 6 周：怀疑自己到底是「会用 AI」还是「被 AI 带着走」
- 第 8 周：扩展模块（RAG/MCP/Agent）开始踩坑，可能想放弃
- 第 10 周：打磨阶段，发现细节比想象中多

这些是正常的。**记住：项目的目的是让你成为一个能讲清楚 AI 工程化的人，不是做一个完美的产品**。

每次想偷懒不写文档、不审 plan、不读代码的时候，问自己一句话：

> 如果面试官现在指着这一行代码问「为什么这么写」，我能讲清楚吗？

讲不清楚就回去补。讲得清楚就继续。

------

**这套工作流就是你接下来 10 周的「方法论锚点」。每周开始前回来读一遍，提醒自己别走偏。**
